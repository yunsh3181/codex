# 파파존스 판교2테크노밸리점 모바일 키오스크

현재 배포 버전은 `kiosk V13`입니다. 프로젝트 운영·배포·보안·변경 내역은 앞으로 이 파일 하나에서 관리합니다.

## 주요 화면

- `index.html`: 고객용 모바일 키오스크
- `admin.html`: 주문 관리자 화면
- `seats.html`: 좌석 현황 화면
- `admin-login.html`: 관리자 로그인
- `assets/images/`: 피자·사이드·음료·초기화면 이미지
- `i18n/`: 한국어·영어·일본어·중국어·베트남어 문구

## 실행

정적 웹 서버의 루트를 이 폴더로 지정한 뒤 `index.html`을 엽니다. 모바일 화면은 아이폰 세로모드 기준으로 확인합니다.

## Windows 키오스크 실행

Electron 셸은 기존 루트의 `index.html`, 이미지, 스타일, 언어팩과 주문 로직을
그대로 로컬에서 불러옵니다. 웹 배포 파일을 `desktop/`에 복제하지 않습니다.

```bash
npm install
npm run desktop:dev
```

개발 모드는 일반 창으로 실행되며 `Ctrl+Q`(macOS는 `Cmd+Q`)로 종료합니다.
운영 빌드는 전체 화면 키오스크로 실행되며 관리자가
`Ctrl+Alt+Shift+Q`를 눌러 종료할 수 있습니다. 고객의 `Ctrl+W`, 일반 창 닫기,
개발자도구 단축키는 차단됩니다.

Windows x64 NSIS 설치 파일과 Portable 실행 파일은 다음 명령으로 만듭니다.

```bash
npm run desktop:build:win
```

결과는 `dist/` 아래의 `PapaJohns-Kiosk-Setup-<version>.exe`와
`PapaJohns-Kiosk-Portable-<version>.exe`이며 Git에는 포함되지 않습니다.

## Firebase

1. `firebase-config.js`에서 대상 Firebase 프로젝트 설정을 확인합니다.
2. Firebase Console의 Firestore Rules에 `firestore.rules` 내용을 적용합니다.
3. 관리자 화면에서 알림음을 활성화한 뒤 테스트 주문을 진행합니다.
4. 주문 접수·조리 시작·조리 완료와 좌석 상태 변경을 확인합니다.

실제 운영에서는 Firebase Authentication 관리자 권한, App Check, 예산 알림과 사용량 알림을 함께 적용하는 것을 권장합니다. 웹 API Key 자체가 보안 경계는 아니며 실제 접근 제어는 Firestore Rules와 Authentication이 담당합니다.

## 보안 주의사항

- 운영 환경에서 공개 읽기·쓰기 테스트 규칙을 사용하지 않습니다.
- 주문·좌석·줄서기·고객·통계 접근 권한은 `firestore.rules`를 기준으로 관리합니다.
- 전화번호는 필요한 최소 정보만 저장하며 관리자 화면의 외부 데이터는 HTML 이스케이프를 유지합니다.
- 가격 위변조 방지를 강화하려면 Cloud Functions에서 공식 메뉴 가격으로 주문 금액을 재계산하는 방식을 권장합니다.

## 버전 변경 내역

### V13

- 곱들이 추가 화면의 수량 숫자를 검정색으로 고정해 가독성 개선
- 결제수단 화면을 아이보리 배경으로 변경하고 결제수단 선택 버튼을 노란색으로 통일
- 선택된 결제수단은 진한 노란색·빨간 테두리·체크 표시로 구분
- 주문 접수 버튼은 결제수단 선택 전 회색, 선택 완료 후 `#E52511` 빨간색으로 활성화

### V12

- 모든 고객용 화면에서 사용하는 파파존스 로고의 빨간 영역을 `#E52511`로 통일
- 기존 로고의 형태·비율·투명 배경·가장자리 투명도 유지
- 포장 카드의 종이백 로고도 승인된 빨간 계열로 통일하고 사진의 조명·질감 유지
- 언어 선택 화면의 로고만 기존 표시 크기의 3배로 확대

### V11

- 식사 구역 심볼을 검정색으로 변경하고 좌석 선택 전 아이보리·선택 후 노란색 적용
- 2·3·4인 세트 사진 설명을 선택 가능한 피자·사이드·탄산 수량 안내로 변경
- 세트 포함 사이드·일반 사이드·토핑 수량 숫자를 검정색으로 통일
- 포장 주문시간 선택 카드를 20% 확대
- 일반·제로 세트 음료를 `REGULAR/ZERO · 음료명과 용량 · 세트 포함` 3단 중앙 정렬로 통일
- 토핑·사이드·음료 선택 화면의 노란색 다음 카드를 상단에 고정

### V10

- 진녹색·흰색·진노랑·회색·빨강 배경별 고대비 글자 색상 규칙 적용
- 초기화면 먹고가기·포장하기와 식사 구역 아이콘 가독성 개선
- 선택된 좌석 카드를 오크 단색으로 변경
- 피자·사이드 메뉴 이름과 진녹색 카드 내부 글자를 흰색으로 통일
- 세트 포함 제로 음료의 검정 영역을 확대하고 `ZERO / 음료명·용량 / 세트 포함` 3단 구성 적용
- 장바구니와 결제수단 선택 화면을 카카오 노란색 배경으로 변경

### V9

- 먹고가기 인원·식사 구역과 전체 선택 카드를 어두운 고대비 디자인으로 통일
- 테이블 선택 카드에 CSS 나무무늬와 상태별 고대비 색상 적용
- 세트 음료 카드를 콜라·제로콜라·스프라이트·제로스프라이트 색상으로 구분
- 음료 수량 `− / +` 버튼을 노란색과 검정 글자로 변경
- 반반 추가금 확대와 반반 선택 확인 버튼 글자 크기 통일
- UP & UP 완료 후 추가 주문 팝업 없이 세트와 동일하게 장바구니로 이동
- 장바구니 하단에 세트·UP & UP·단품 추가와 주문하기 버튼 배치
- 장바구니 화면에서는 처음으로·이전 버튼을 표시하지 않음

### V8

- 언어 선택 카드 영역을 화면 중앙에 배치하고 좌우 여백을 동일하게 조정
- 초기화면·언어 선택·주문 진행 등 모든 페이지의 파파존스 로고 배경을 투명하게 통일

### V7

- 장바구니 상품 정보를 `사이즈 - 크러스트 - 도우 - 구성 - 피자` 순으로 정리
- 한판 주문의 `한판` 문구를 숨기고 반반 주문에만 `반반` 표시
- 기본 도우의 고객 표시 명칭을 `클래식`으로 변경
- 모든 한국어 `반반` 문구를 `반반`으로 통일
- 초기화면 로고 20% 축소, 투명 배경과 좌측 상단 배치 적용
- 로고 아래 매장명을 `판교2테크노밸리점`으로 축약
- 우측 상단 언어 버튼을 `LANGUAGE`로 변경
- 주문 접수 완료 아이콘을 파파존스 빨간색으로 변경
- 기존 크러스트 선택 화면과 주문 진행 로직은 유지

### V6

- 해피아워 시간 제어, 음료 용량별 `− / +`, 모바일 하단 스크롤 정리
- 4인 세트 이미지 배열, 노란 주문확인 버튼, 빨간 크러스트 배지 적용
- 주문 접수 완료 문구를 검은색으로 변경

### V5

- 초기화면 진한 그린 배경과 회색 배너 적용
- 바로주문·예약주문 카드와 2·3·4인 세트 배열 확대
- 사이드·곁들이 카드 터치 수량과 전화번호 상태 색상 적용

### V4

- 승인된 파파존스 로고·피자·사이드 이미지와 통합 카드 디자인 적용
- 다음 노란색, 이전 주황색, 하단 합계 녹색으로 통일

### V3

- 승인된 콜라·스프라이트 카드 이미지와 용량별 수량 표시 적용

### V2

- 세트 카드 잘림, 사이드 복귀 오류와 토핑·음료·곁들이 흐름 개선

### V1

- 초기 모바일 화면, 언어 버튼, 주문시간 카드와 혜택 화면 개선

## 유지관리 원칙

- 고객 주문·가격·결제 로직을 변경할 때는 모바일 화면과 주문 검토 화면을 함께 테스트합니다.
- 신규 이미지 적용 전 별도 시안을 승인받습니다.
- 향후 안내와 변경 내역은 새 문서를 만들지 않고 이 `README.md`에 누적합니다.

## Windows 키오스크 로컬 런타임

고객 주문 화면은 Electron `file://`과 GitHub Pages의 저장소 하위 경로에서 같은
`index.html`, 메뉴 데이터, 언어팩, CSS, JavaScript, 이미지를 사용합니다. Firebase
SDK 10.12.5 compat 배포 파일도 `assets/vendor/firebase/`에 고정해 앱 시작과 메뉴
탐색에 CDN 연결이 필요하지 않습니다. Firestore 주문 저장, 좌석, 대기열 기능은
계속 온라인 연결이 필요하며 실패 주문을 로컬 저장하거나 자동 재전송하지 않습니다.

개발 모드(`npm run desktop:dev`)에서는 Electron 콘솔에 URL의 origin/path, 도메인,
리소스 종류, Firebase 여부만 기록합니다. 쿼리 문자열, 전화번호, 주문 payload는
기록하지 않으며 운영 빌드에서는 이 로그와 개발자도구를 사용하지 않습니다.

### 고객 키오스크 외부 리소스 조사

| 외부 리소스 URL | 사용 파일 | 사용 목적 | 로컬화 | 유지 이유 | 변경 방법 |
| --- | --- | --- | --- | --- | --- |
| `https://www.gstatic.com/firebasejs/10.12.5/firebase-app-compat.js` | `index.html` | Firebase 앱 초기화 | 가능 | 없음 | 동일 버전을 `assets/vendor/firebase/`에서 로드 |
| `https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore-compat.js` | `index.html` | Firestore compat API | 가능 | 없음 | 동일 버전을 `assets/vendor/firebase/`에서 로드 |
| `*.googleapis.com`, Firebase 프로젝트 엔드포인트 | Firebase SDK 런타임 | 주문 저장, 좌석·대기열 동기화 | 불가 | 서버 데이터 통신 | 온라인 전용으로 유지하고 오류 콜백 및 사용자 안내 적용 |

고객 `index.html`에는 외부 이미지, CSS CDN, 웹폰트, 아이콘, JSON, analytics,
tracking 요청이 없습니다. 메뉴는 인라인 `window.KIOSK_DATA`, 언어팩은 `i18n/*.js`,
이미지는 `assets/images/` 상대경로를 공유합니다.
