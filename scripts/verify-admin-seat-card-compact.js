const {app,BrowserWindow,nativeImage}=require('electron');
const fs=require('node:fs'),os=require('node:os'),path=require('node:path');
const {exportAdminVisualSite}=require('./serve-admin-visual');
const {runElectronVerification}=require('./electron-verification-lifecycle');
const root=path.resolve(__dirname,'..'),report=process.env.ADMIN_SEAT_CARD_REPORT,screens=process.env.ADMIN_SEAT_CARD_SCREENSHOTS;
if(!report||!screens)throw new Error('ADMIN_SEAT_CARD_REPORT and ADMIN_SEAT_CARD_SCREENSHOTS are required');
app.disableHardwareAcceleration();app.commandLine.appendSwitch('headless');app.commandLine.appendSwitch('force-device-scale-factor','1');
const delay=ms=>new Promise(resolve=>setTimeout(resolve,ms));
async function measure(win){return win.webContents.executeJavaScript(`(()=>{const cards=[...document.querySelectorAll('.seat-overview-card')],overlap=(a,b)=>{const x=a.getBoundingClientRect(),y=b.getBoundingClientRect();return x.left<y.right&&x.right>y.left&&x.top<y.bottom&&x.bottom>y.top},selectors={name:'strong',status:'.seat-overview-status',elapsed:'.seat-occupied-elapsed',party:'.seat-party-size',order:'small:not(.seat-occupied-elapsed):not(.seat-party-size)',button:'.admin-seat-action'},clipped={};for(const [key,selector] of Object.entries(selectors))clipped[key]=cards.flatMap(card=>[...card.querySelectorAll(selector)]).filter(node=>node.scrollWidth>node.clientWidth||node.scrollHeight>node.clientHeight).length;const internalOverlap=cards.filter(card=>{const nodes=[...card.children].filter(node=>getComputedStyle(node).display!=='none');return nodes.some((node,index)=>nodes.slice(index+1).some(other=>overlap(node,other)))}).length,action=document.querySelector('.admin-seat-action'),actions=document.querySelector('.admin-seat-actions'),name=document.querySelector('.seat-overview-card>strong'),status=document.querySelector('.seat-overview-status'),small=document.querySelector('.seat-overview-card small'),right=document.querySelector('.seat-overview').getBoundingClientRect(),style=getComputedStyle(action);return {viewport:[innerWidth,innerHeight],cards:cards.length,rightWidth:Math.round(right.width),horizontalOverflow:Math.max(0,document.documentElement.scrollWidth-document.documentElement.clientWidth),clipped,internalOverlap,verticalKorean:cards.filter(card=>card.querySelector('strong').getBoundingClientRect().width<parseFloat(getComputedStyle(card.querySelector('strong')).fontSize)*2).length,twoButtonRows:[...document.querySelectorAll('.admin-seat-actions')].filter(row=>row.children.length===2&&Math.abs(row.children[0].getBoundingClientRect().top-row.children[1].getBoundingClientRect().top)<1).length,button:{minHeight:style.minHeight,padding:[style.paddingTop,style.paddingRight,style.paddingBottom,style.paddingLeft],fontSize:style.fontSize,lineHeight:style.lineHeight,borderRadius:style.borderRadius,gap:getComputedStyle(actions).gap},fonts:{name:getComputedStyle(name).fontSize,status:getComputedStyle(status).fontSize,small:getComputedStyle(small).fontSize},fixture:Object.fromEntries(['papa-2','papa-bar4','outdoor-1','outdoor-2','outdoor-3','outdoor-4','annex-1'].map(id=>{const card=document.querySelector('[data-seat-id="'+id+'"]');return [id,{text:card.textContent.replace(/\\s+/g,' ').trim(),actions:[...card.querySelectorAll('.admin-seat-action')].map(button=>button.textContent.trim())}]}))}})()`)}
async function capture(win,name){const image=await win.webContents.capturePage();fs.mkdirSync(screens,{recursive:true});fs.writeFileSync(path.join(screens,`${name}.png`),nativeImage.createFromBuffer(image.toPNG()).toPNG())}
async function main(lifecycle){lifecycle.expectReport(report);const site=fs.mkdtempSync(path.join(os.tmpdir(),'admin-seat-card-'));exportAdminVisualSite(site);const win=lifecycle.trackWindow(new BrowserWindow({show:true,width:1440,height:900,webPreferences:{contextIsolation:false,nodeIntegration:false,sandbox:false,backgroundThrottling:false}}));try{await win.loadFile(path.join(site,'admin','index.html'));await win.webContents.executeJavaScript(`new Promise(resolve=>{const done=()=>document.querySelectorAll('.seat-overview-card').length===13?resolve():setTimeout(done,20);done()})`);await win.webContents.executeJavaScript(`(()=>{const now=Date.now();PJAdminVisualFixture.setSeat('papa-2',{status:'empty'});PJAdminVisualFixture.setSeat('papa-bar4',{status:'occupied',occupiedAt:new Date(now)});PJAdminVisualFixture.setSeat('outdoor-1',{status:'occupied',occupiedAt:new Date(now-59*60000)});PJAdminVisualFixture.setSeat('outdoor-2',{status:'occupied',occupiedAt:new Date(now-65*60000)});PJAdminVisualFixture.setSeat('outdoor-3',{status:'reserved',reservationPartySize:4,orderNo:'R-105'});PJAdminVisualFixture.setSeat('outdoor-4',{status:'held',orderNo:'H-204'});PJAdminVisualFixture.setSeat('annex-1',{status:'mystery'});return true})()`);await delay(80);const result={};for(const [width,height] of [[1100,800],[1440,900],[1920,1080]]){await win.webContents.debugger.attach('1.3');await win.webContents.debugger.sendCommand('Emulation.setDeviceMetricsOverride',{width,height,deviceScaleFactor:1,mobile:false});await win.webContents.debugger.detach();await delay(80);result[`${width}x${height}`]=await measure(win);await capture(win,`admin-seat-card-${width}x${height}`)}await lifecycle.writeReportAtomically(report,result)}finally{fs.rmSync(site,{recursive:true,force:true})}}
runElectronVerification({app},main);
