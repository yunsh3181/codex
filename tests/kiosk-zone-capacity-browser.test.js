const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const os=require('node:os');const path=require('node:path');const {spawnElectronVerificationSync,assertElectronSucceeded}=require('./helpers/electron-verification-process');
const root=path.resolve(__dirname,'..');
test('capacity states and six locales render and behave in Chromium',{timeout:180000},t=>{const temp=fs.mkdtempSync(path.join(os.tmpdir(),'kiosk-zone-capacity-')),report=path.join(temp,'report.json'),screens=path.join(root,'artifacts','kiosk-zone-capacity'),profile=path.join(temp,'profile');t.after(()=>fs.rmSync(temp,{recursive:true,force:true}));const run=spawnElectronVerificationSync(['scripts/verify-kiosk-zone-capacity.js'],{cwd:root,encoding:'utf8',env:{...process.env,KIOSK_ZONE_REPORT:report,KIOSK_ZONE_SCREENSHOT_DIR:screens,ELECTRON_VERIFICATION_USER_DATA:profile,ELECTRON_DISABLE_SECURITY_WARNINGS:'true'},timeout:170000,maxBuffer:10*1024*1024});assertElectronSucceeded(assert,run,report);const value=JSON.parse(fs.readFileSync(report,'utf8'));assert.deepEqual(value.interaction,{card:0,queue:1,focused:true});assert.deepEqual(value.transitions,{values:['available','full','available','review','available','available'],writes:0});for(const row of value.results.filter(x=>x.kind==='full')){assert.equal(row.horizontalOverflow,0);assert.deepEqual(row.clipping,[]);assert.deepEqual(row.vertical,[]);assert.ok(row.fullText);assert.equal(row.buttonDisabled,true);assert.ok(row.queueLabel);assert.equal(row.queueFocusable,true)}for(const row of value.navigation.rows){assert.deepEqual(row.back,[...row.visited].slice(0,-1).reverse(),row.name);assert.equal(row.cartPreserved,true,row.name);assert.equal(row.totalPreserved,true,row.name);assert.equal(row.releaseWrites+row.additionalHolds+row.orderWrites+row.paymentCalls,0,row.name)}assert.deepEqual(value.navigation.nonSteps.after,value.navigation.nonSteps.before);assert.equal(value.kiosk.horizontalOverflow,0);for(const name of ['available','full','offhours','weekend','holiday','review'])assert.ok(fs.statSync(path.join(screens,`ipad-air3-${name}-834x1112.png`)).size>10000);assert.ok(fs.statSync(path.join(screens,'kiosk-full-1080x1920.png')).size>10000)});
